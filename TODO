( ) Clamp Food position so that it never spawns on the edges
( ) Add game over when hit edge of screen
( ) Add game over screen
( ) Add touch controls
( ) Fix problem: When food spawns in snake
( ) Add username input 
( ) Add high score tracking
( ) Add leaderboard backend
( ) Toggle sounds
( ) Change users through settings